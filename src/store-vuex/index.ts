import { createStore } from 'vuex';

export function createMyStore() {
  return createStore({
    state: {},
    mutations: {},
    actions: {},
    modules: {}
  });
}

export default createStore({
  state: {},
  mutations: {},
  actions: {},
  modules: {}
});
